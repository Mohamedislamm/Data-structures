This project develops an autonomous web-crawling system that populates a localized positional inverted index by harvesting Wikipedia articles. It transforms raw text into structured numerical vectors using TF-IDF weighting and the Porter Stemming algorithm to ensure semantic accuracy. Finally, the engine ranks document relevance by calculating the cosine similarity between user queries and stored content, normalized by document length for optimized search precision.
